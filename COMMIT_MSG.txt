feat(research): draft tracking dataview and quick-actions draft creation

- Add zk-research-ai-pipeline-tracking dataview script for draft status
  - Table: Draft (link), Title, Status, Section, Modified
  - Queries project drafts folder, sorted by mtime
- Add Draft row to zk-research-quick-actions: text field + Create button
  - Calls createResearchDraft(projectPath, title) to create note in drafts/
- Expose createResearchDraft on ZettelkastenOperator for dataview scripts
- Dashboard already has section "1. AI Pipeline Tracking (Draft Status)"
  and "2. Atomic Intelligence" with ai-pipeline + atomic-notes
- Tests: include zk-research-ai-pipeline-tracking in manager expected scripts
